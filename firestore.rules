
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Fully open rules for development as requested
      allow read, write: if true;
    }
  }
}
